<Page
    x:Class="DKOctoberTablet.Pages.MainPageButtons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="using:DKOctoberTablet.Pages"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <pages:Header Grid.Row="0" />
        <Grid Row="1" Margin="150 0 150 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Grid.Row="0"
                        Background="HotPink" 
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Margin="30" CornerRadius="5"
				        Tapped="InfoButtonTapped">
                <TextBlock FontSize="60" FontWeight="SemiBold" Foreground="White" Text="Информация" />
            </Button>
            <Button Grid.Column="1" Grid.Row="0" Background="HotPink"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Margin="30" CornerRadius="5"
			        Tapped="AdministrationButtonTapped">
                <TextBlock FontSize="60" FontWeight="SemiBold" Foreground="White" Text="Администрация" />
            </Button>
            <Button Grid.Column="0" Grid.Row="1" Background="HotPink"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Margin="30" CornerRadius="5"
			        Tapped="ScheduleButtonTapped">
                <TextBlock FontSize="60" FontWeight="SemiBold" Foreground="White" Text="Расписание" />
            </Button>
            <Button Grid.Column="1" Grid.Row="1" Background="HotPink"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Margin="30" CornerRadius="5"
			        Tapped="PosterButtonTapped">
                <TextBlock FontSize="60" FontWeight="SemiBold" Foreground="White" Text="Афиша" />
            </Button>
            <Button Grid.Column="0" Grid.Row="2" Background="HotPink"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Margin="30" CornerRadius="5"
			        Tapped="AuditoriumButtonTapped">
                <TextBlock FontSize="60" FontWeight="SemiBold" Foreground="White" Text="Зрительный зал" />
            </Button>
            <Button Grid.Column="1" Grid.Row="2" Background="HotPink"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Margin="30" CornerRadius="5"
			        Tapped="GalleryButtonTapped">
                <TextBlock FontSize="60" FontWeight="SemiBold" Foreground="White" Text="Галерея" />
            </Button>
        </Grid>
    </Grid>
</Page>
